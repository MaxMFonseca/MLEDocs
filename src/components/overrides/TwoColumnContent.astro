<div class="mle-document-columns">
	{
		Astro.locals.starlightRoute.toc && (
			<aside class="mle-page-outline print:hidden">
				<slot name="right-sidebar" />
			</aside>
		)
	}
	<div class="main-pane"><slot /></div>
</div>

<style>
	@layer starlight.core {
		.main-pane {
			isolation: isolate;
			min-width: 0;
			width: 100%;
		}

		:global(html[data-has-toc]) {
			--sl-mobile-toc-height: 3rem;
		}

		@media (min-width: 95rem) {
			:global(html[data-has-toc]) {
				--sl-mobile-toc-height: 0rem;
			}

			.mle-document-columns {
				display: flex;
				align-items: stretch;
			}

			.mle-page-outline {
				order: 2;
				flex: 0 0 14rem;
				width: 14rem;
				border-inline-start: 1px solid var(--sl-color-hairline);
			}

			.main-pane {
				order: 1;
				width: calc(100% - 14rem);
			}
		}
	}
</style>
